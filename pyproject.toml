[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "reasoning"
version = "0.1.0"
description = "Utilities and scripts for reasoning and refusal behavior experiments."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
